apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: paperless

resources:
  - data-pvc.yaml
  - media-pvc.yaml

  - configmap.yaml
  - secret.sealed.yaml
  - service.yaml

  - deployment.yaml
  - ingress-route.yaml
